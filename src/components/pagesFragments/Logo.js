import React from "react";

const MyLogo = () => {
  return (
    <div className="logo">
      <p>&lt;this.is.louie&gt;</p>
    </div>
  );
};
export default MyLogo;
/*COMPONENT DESCRIPTION
parent: LandingPage and all the projectpages;
role: providing a logo; I left it as a 'placeholder' because so far I could not come up with a logo I like
so right now the logo is a text only, appears only if the mouse is hovered over it
*/